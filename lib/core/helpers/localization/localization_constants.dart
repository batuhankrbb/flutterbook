import 'package:flutter/material.dart';

class LocalizationConstants {
  LocalizationConstants._();
  
  static var enUSLocale = const Locale("en", "US");
  static var fallBackLocale = enUSLocale;
  static var localizationPath = "assets/translations";
}
